function initTypingEffect() {
      new Typed(".typedText", {
            strings: ["Desenvolvedor", "Front-end", "Back-end", "Fullstack"],
            loop: true,
            typeSpeed: 100,
            backSpeed: 80,
            backDelay: 2000
      });
}

initTypingEffect();


